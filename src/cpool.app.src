%% Feel free to use, reuse and abuse the code in this file.

{application, cpool, [
	{description, "cpool v2.0."},
	{vsn, "2.0"},
	{modules, []},
	{registered, [cpool_sup]},
	{applications, [
		kernel,
		stdlib,
		crypto,
		public_key,
		ssl,
		cowboy
	]},
	{mod, {cpool, []}},
	{env, []}
]}.
